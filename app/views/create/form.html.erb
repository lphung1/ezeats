<%= form_for @recipes do |f| %>
 
  <% if @recipes.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@recipes.errors.count, "error") %> prohibited
        this recipes from being saved:
      </h2>
      <ul>
        <% @recipes.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
 
  <p>
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </p>
 
  <p>
    <%= f.label :ingredient %><br>
    <%= f.text_area :ingredient %>
  </p>
 
  <p>
    <%= f.submit %>
  </p>
 
<% end %>